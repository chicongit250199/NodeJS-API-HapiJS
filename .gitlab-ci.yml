cache:
  key: '$CI_BUILD_REF_NAME node:8'
  paths:
    - node_modules/

stages:
  - test

docker-test:
  stage: test
  image: node:8
  before_script:
    - rm -rf /tmp/*
    - npm install -g eslint
    - npm install --no-cache
  script:
    - npm run lint
  only:
    - deploy
